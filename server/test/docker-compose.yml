services:
  myserver:
    build: .
    ports:
      - 9999:9999
    volumes:
      - ./src/:/myserver/
    command: 
      bash -c "cd /myserver && gcc -O3 serverTest.c -o serverTest -lm -I/usr/include/postgresql -lpq && ./serverTest"
    depends_on:
      - db
  db:
    image: postgres:18.3
    restart: always
    shm_size: 128mb
    ports:
      - 5432:5432
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: pass4root
      POSTGRES_USER: root
  adminer:
    image: adminer:5.4.2
    restart: always
    ports:
      - 8080:8080

volumes:
  db-data: {}